import { MinimalShellPlaceholder } from "../shell/minimal-shell-placeholder";

export function ActivityPlaceholder() {
  return (
    <MinimalShellPlaceholder
      title="Activity"
      lead="Not built yet — recent activity and history will appear here when shipped."
    />
  );
}
