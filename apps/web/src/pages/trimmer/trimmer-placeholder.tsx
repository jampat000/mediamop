import { MinimalShellPlaceholder } from "../shell/minimal-shell-placeholder";

export function TrimmerPlaceholder() {
  return (
    <MinimalShellPlaceholder
      title="Trimmer"
      lead="Not built yet — trimming will use this route when the module ships."
    />
  );
}
