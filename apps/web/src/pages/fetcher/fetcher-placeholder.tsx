import { MinimalShellPlaceholder } from "../shell/minimal-shell-placeholder";

export function FetcherPlaceholder() {
  return (
    <MinimalShellPlaceholder
      title="Fetcher"
      lead="Not built yet — fetch workflows will use this route when the module ships."
    />
  );
}
