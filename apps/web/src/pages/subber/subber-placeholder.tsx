export function SubberPlaceholder() {
  return (
    <div className="mm-page">
      <header className="mm-page__intro">
        <p className="mm-page__eyebrow">MediaMop</p>
        <h1 className="mm-page__title">Subber</h1>
        <p className="mm-page__subtitle">
          Subber will be MediaMop’s module for subtitle and caption workflows — sourcing, aligning, and managing text
          tracks alongside your media, independent of the other modules.
        </p>
        <p className="mm-page__lead">Not built yet — this route is reserved for when the module ships.</p>
      </header>
    </div>
  );
}
