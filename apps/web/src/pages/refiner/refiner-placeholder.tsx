import { MinimalShellPlaceholder } from "../shell/minimal-shell-placeholder";

export function RefinerPlaceholder() {
  return (
    <MinimalShellPlaceholder
      title="Refiner"
      lead="Not built yet — refinement tools will use this route when the module ships."
    />
  );
}
