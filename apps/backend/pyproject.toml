[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mediamop-backend"
version = "2.6.5"
description = "MediaMop backend spine (FastAPI, SQLite, Alembic)"
readme = "README.md"
requires-python = ">=3.11"
license = "AGPL-3.0-or-later"
dependencies = [
  "fastapi>=0.115,!=0.136.3",
  "python-dotenv>=1.0",
  "uvicorn>=0.32",
  "sqlalchemy>=2.0.40",
  "alembic>=1.14",
  "argon2-cffi>=23.1.0",
  "itsdangerous>=2.1",
  "cryptography>=44.0.0",
  "httpx>=0.27",
  "tzdata>=2025.1",
  # Filesystem events are the trigger for Refiner work; the periodic scan is the
  # backstop. Import failure is handled as a fallback to polling, not as a hard
  # requirement, because container mounts and network shares often deliver no events.
  "watchdog>=4.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "playwright>=1.49.0",
  "ruff>=0.4",
  "mypy>=1.10",
  "pytest-cov>=5.0",
  "pytest-asyncio>=0.23",
  "httpx>=0.27",
  # Starlette 1.x uses this compatibility package for its TestClient; keeping it
  # in the dev set removes the deprecated httpx 0.x adapter and its warning.
  "httpx2>=2.12.0",
  "bandit>=1.7",
  "pip-audit>=2.7",
]
build = [
  "pillow>=11.0.0",
  "pyinstaller>=6.12.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["mediamop*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82", "B", "C4", "I", "UP", "SIM"]
ignore = ["B008"]

[tool.ruff.lint.per-file-ignores]
"tests/test_csrf_unit.py" = ["C408"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
follow_imports = "normal"
check_untyped_defs = true
warn_unused_ignores = true
ignore_errors = false

[[tool.mypy.overrides]]
# ctypes.WinDLL / windll / get_last_error and os.startfile are Windows-only APIs.
# type: ignore[attr-defined] comments on those lines suppress CI (Linux) failures,
# but on Windows mypy knows the attributes so the comments become "unused-ignore".
# This narrow override disables only that false-positive for windows-specific code.
module = ["mediamop.windows.*"]
disable_error_code = ["unused-ignore"]

